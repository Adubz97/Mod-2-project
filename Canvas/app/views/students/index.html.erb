<div class="container">
  <div class="card border-0 shadow my-5">
    <div class="card-body p-4">
      <h1 class="font-weight-light">  Students:  </h1>
          <hr class="my-4">

<h5>
<ul>
<% @students.each do |student| %>
  <li><%= link_to student.name, student_path(student) %> -<%= student.major %></li>
<% end %>
</ul>
</h5>

</div>
      </div>
    </div>